enum TransactionType {
  EXPENSE,
  INCOME
}

enum ChartType {
  CIRCLE,
  LINEAR,
  BAR
}

enum HistoryType {
  DEBT,
  MONEYBOX,
  USER,
  GROUP
}

enum AddNameType { DEBT }

enum AddAmountType { DEBT }
